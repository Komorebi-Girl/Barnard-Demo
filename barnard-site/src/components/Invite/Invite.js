import React, { Component } from "react";

const inviteStyle = {
  color: "#3887F7",
  fontSize: 64,
  fontFamily: "Lato"
};

class Invite extends Component {
  render() {
    return (
      <div className="row justify-content-center Invite" style={inviteStyle}>
        What We Offer
      </div>
    );
  }
}

export default Invite;
